# Core dependencies
requests>=2.25.1
youtube-transcript-api>=0.5.0
nltk>=3.6
textwrap3>=0.9.2
yt-dlp>=2023.3.4
beautifulsoup4>=4.12.0
lxml>=4.9.0
feedparser>=6.0.10

# AI provider libraries (no Ollama needed for deployment)
openai>=1.12.0
anthropic>=0.25.0
tiktoken>=0.5.0
python-dotenv>=1.0.0

# Streamlit UI
streamlit>=1.20.0

# Remove faster-whisper for deployment (too heavy, CPU-intensive)
# If you need transcription, use API-based services instead
